# Tasks

## 1. Shared HTTP layer

- [ ] 1.1 Create `frontend/src-tauri/src/llm/mod.rs`, `llm/client.rs`, `llm/error.rs`; register `pub mod llm;` in `frontend/src-tauri/src/lib.rs`; add `LlmError` enum (`Timeout{seconds}`, `Connect(String)`, `Http{status,body}`, `AuthFailed{status,body}`, `Decode(String)`, `Cancelled`) with a `Display` impl matching today's strings (`"LLM request timed out after {seconds} seconds"`, `"Failed to send request to LLM: {msg}"`, `"LLM API request failed: {body}"`) and a `.to_string()`/`From<LlmError> for String` conversion; verify with `cargo check -p meetily`.
- [ ] 1.2 Implement `pub fn shared_client() -> &'static reqwest::Client` in `llm/client.rs` using `once_cell::sync::Lazy`, with `.connect_timeout(Duration::from_secs(10))`, `.timeout(Duration::from_secs(300))`, `.user_agent(concat!("meetily/", env!("CARGO_PKG_VERSION")))`; verify with a unit test asserting two calls to `shared_client()` return the same client (e.g. compare `Arc`-level pointer via a wrapping `Lazy` check, or assert no panic/rebuild via a call counter in the builder closure).
- [ ] 1.3 Implement `pub struct RetryPolicy { max_retries: u32, base_backoff: Duration, max_backoff: Duration }` with `Default` (`max_retries: 2`, matching Ollama's current constant) and a `short()` constructor for model-listing call sites; verify with `cargo check -p meetily`.
- [ ] 1.4 Implement `pub async fn send_with_retry(build_request: impl Fn() -> reqwest::RequestBuilder, policy: &RetryPolicy) -> Result<reqwest::Response, LlmError>`: classify each attempt via `reqwest::Error::is_timeout()`/`is_connect()` (retryable), `status() == 429` or `status().is_server_error()` (retryable), `status() == 401 || status() == 403` (return `LlmError::AuthFailed` immediately), other 4xx (return `LlmError::Http` immediately); apply exponential backoff with jitter between retries, capped at `policy.max_backoff`; verify with `cargo check -p meetily`.
- [ ] 1.5 Add `wiremock = "0.5"` under `[dev-dependencies]` in `frontend/src-tauri/Cargo.toml`; verify with `cargo check -p meetily --all-targets`.
- [ ] 1.6 Write unit tests in `llm/client.rs` using `wiremock::MockServer`: (a) a 503-then-200 mock sequence returns the 200 body via `send_with_retry`; (b) a 401 mock returns `LlmError::AuthFailed` after exactly one request received by the mock server (assert via `MockServer::received_requests()`); (c) a mock that never responds within a short test timeout returns `LlmError::Timeout` after the bounded attempts, with elapsed wall time asserted below a generous upper bound; verify with `cargo test -p meetily --lib llm`.

## 2. Migrate `summary/llm_client.rs` and `summary/service.rs`

- [ ] 2.1 In `summary/service.rs:535`, delete `let client = reqwest::Client::new();` and pass `crate::llm::shared_client()` into `generate_meeting_summary`; verify with `cargo check -p meetily`.
- [ ] 2.2 In `summary/llm_client.rs::generate_summary` (lines ~115-330), replace the direct `client.post(api_url).headers(headers).json(&request_body).timeout(REQUEST_TIMEOUT_DURATION).send()` with `crate::llm::send_with_retry(|| client.post(&api_url).headers(headers.clone()).json(&request_body), &RetryPolicy::default())`, keeping the existing `tokio::select!` cancellation wrapper around the whole retrying future; map the returned `LlmError` to the existing debug-log `DebugLogResult::Error` path and to the function's `Result<String, String>` return type using `LlmError::to_string()`; verify with `cargo check -p meetily`.
- [ ] 2.3 Fix the stale timeout message: remove the hardcoded `"LLM request timed out after 60 seconds"` strings (llm_client.rs:280, 301) now that `LlmError::Timeout{seconds}` reports the real `REQUEST_TIMEOUT_DURATION` value; verify by grepping the file for the string `"60 seconds"` and confirming zero matches.
- [ ] 2.4 Run the existing summary-generation tests (if any) and a manual sanity check: `cargo test -p meetily --lib summary`; verify no regressions.

## 3. Migrate provider model-listing call sites

- [ ] 3.1 In `ollama/ollama.rs`, delete `get_models_via_http_with_retry` (lines 126-157) and its `MAX_RETRIES`/`INITIAL_BACKOFF_MS` constants; change `get_models_via_http_async` (line 162) to build its request via `crate::llm::shared_client()` and call `crate::llm::send_with_retry(..., &RetryPolicy::short())`; keep the outer `get_ollama_models`'s 5s `tokio::time::timeout` wrapper and CLI-fallback behavior unchanged; verify with `cargo check -p meetily` and `cargo test -p meetily --lib ollama` (if existing tests cover this path) or a manual `pull_ollama_model`/`get_ollama_models` smoke check against a local Ollama instance if available.
- [ ] 3.2 In `ollama/ollama.rs`, change the `Client::new()` in `pull_ollama_model` (line 287) and `delete_ollama_model` (line 458) to `crate::llm::shared_client().clone()` (no retry wrapper); verify with `cargo check -p meetily`.
- [ ] 3.3 In `openai/openai.rs:130` (`get_openai_models`), replace `reqwest::Client::new()` with `crate::llm::shared_client()` and wrap the `.get(...).send()` call in `crate::llm::send_with_retry(..., &RetryPolicy::short())`; keep the existing "fall back to hardcoded models" behavior on any resulting error; verify with `cargo check -p meetily`.
- [ ] 3.4 Repeat 3.3 for `anthropic/anthropic.rs:100` (`get_anthropic_models`) and `groq/groq.rs:98` (`get_groq_models`); verify with `cargo check -p meetily`.
- [ ] 3.5 In `openrouter/openrouter.rs:43`, convert `get_openrouter_models` from `pub fn` to `pub async fn`, replace `reqwest::blocking::Client` with `crate::llm::shared_client()` and `crate::llm::send_with_retry(..., &RetryPolicy::short())`; update its `#[tauri::command]` registration if `generate_handler!` requires any change for async commands (it typically does not); verify with `cargo check -p meetily` and confirm the command still appears correctly in `lib.rs`'s `generate_handler!` list.

## 4. Verification and cleanup

- [ ] 4.1 Run `cargo clippy -p meetily --all-targets --message-format=short` and fix any new warnings introduced by this change (pre-existing warnings out of scope, per change 03); verify zero new warnings attributable to `llm/`, `ollama/`, `openai/`, `anthropic/`, `groq/`, `openrouter/`, or `summary/llm_client.rs`/`summary/service.rs`.
- [ ] 4.2 Run `cargo test -p meetily --lib llm` and `cargo test -p meetily --lib` (full lib test suite) to confirm no regressions; verify all pass.
- [ ] 4.3 Grep the five provider files and `summary/llm_client.rs`/`summary/service.rs` for `Client::new()` and `reqwest::blocking::Client`; verify the only remaining direct client constructions are the two explicitly-scoped-out sites in `ollama/ollama.rs` (`pull_ollama_model`, `delete_ollama_model`) using `shared_client()` (not `Client::new()`), i.e. zero remaining ad hoc `Client::new()` calls anywhere in these files.
- [ ] 4.4 Run `openspec validate 10-shared-llm-http-client --strict`; verify it passes.
