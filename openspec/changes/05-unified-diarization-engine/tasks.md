# Tasks

Standing verification for every task in sections 1-3 (move-only and de-duplication): `cargo test -p meetily --lib diarization` reports the **same number of passing tests as the baseline recorded in task 1.1** (no test added, removed, renamed out of the filter, or silently skipped), and `cargo clippy -p meetily --all-targets --message-format=short` introduces no new warning. Each task is one move; do not combine moves.

## 1. Create the tree and relocate (no logic change)

- [ ] 1.1 Record the baseline before touching anything: run `cargo test -p meetily --lib diarization` and `cargo clippy -p meetily --all-targets --message-format=short`, and write the passing-test count and the warning count into this line as the reference for every later task; verify: both commands complete and the two numbers are recorded here (today the filter covers the test modules of `audio/diarization.rs` 35, `audio/online_diarization.rs` 17, and `audio/live_diarization_reconcile.rs` 11 test functions).
- [ ] 1.2 Create `frontend/src-tauri/src/audio/diarization/mod.rs` declaring the empty submodules (`config`, `core`, `batch`, `streaming`, `identity`, `persist`, `telemetry`, `commands`, `engine`) and re-exporting, via `pub use`, every item currently re-exported from `audio/mod.rs:154-168`; move `audio/diarization.rs` to `audio/diarization/legacy.rs` re-exported from `mod.rs` as a temporary landing spot; verify: `cargo check -p meetily` passes with no edit to any of the 12 files that reference `audio::diarization::*`, and the standing verification holds.
- [ ] 1.3 Move `ClustererKindSetting`, `DiarizationConfig`, its atomics, `store_f32_option`/`load_f32_option`/`stored_*`/`kind_code`, `set_clustering_overrides`, and `fixed_pool_size` (legacy.rs, from `diarization.rs:569-620` and `623-804`) into `config.rs`, with their unit tests; verify: standing verification, and `diarize_eval` still builds (`cargo check -p meetily --bins`).
- [ ] 1.4 Move `DiarizationSegment`, `ClusteredEmbedding`, `ChannelClusters`, `PolyvoiceDiarizer`, `DiarizationProgress`, `DiarizationResult` (`diarization.rs:54-66`, `1150-1190`) into `core/units.rs`; verify: standing verification.
- [ ] 1.5 Move `build_clusterer` (`diarization.rs:1191-1229`) into `core/cluster.rs` with its tests (`clusterer_factory_enforces_clamped_ceiling`, `merge_threshold_is_inert_under_automatic_count`, `vbx_kind_fails_actionably_on_the_192d_enhanced_family`); verify: standing verification.
- [ ] 1.6 Move `DenseUnit`, `ChunkRecord`, `expand_embed_units`, `embed_unit_slices`, and `V2Core` (`diarization.rs:1396-1722`) into `core/segment.rs`; verify: standing verification, and `parity_stream_vs_in_memory_core` still passes by name.
- [ ] 1.7 Move `assemble_channel_turns`, `subtract_span`, `gap_fill_turns` (`diarization.rs:1723-1999`) into `core/turns.rs` with the `gap_fill_*` tests; verify: standing verification.
- [ ] 1.8 Move `create_polyvoice_diarizer`, `create_polyvoice_diarizer_for_app`, `standalone_model_candidates`, `resolve_models_dir_standalone`, `create_diarizer_standalone`, `diarize_wav_samples` (`diarization.rs:1230-1394`) into `core/factory.rs`; verify: standing verification and `cargo check -p meetily --bins` (the harness imports `diarize_wav_samples` at `bin/diarize_eval.rs:9`).
- [ ] 1.9 Move `PcmStream`, `read_f32_le`, `spawn_ffmpeg_pcm`, `StreamWindows` (`diarization.rs:2082-2283`) into `batch/pcm.rs` with the `read_f32_le_*` and `stream_windows_*` tests; verify: standing verification.
- [ ] 1.10 Move `run_chunked_polyvoice_diarization` and `channel_chunks` (`diarization.rs:2000-2081`) into `batch/chunking.rs` with `chunk_splitting_preserves_overlap_and_offsets`; verify: standing verification.
- [ ] 1.11 Move `DiarizationGuard` with `DIARIZATION_IN_PROGRESS`/`DIARIZATION_CANCELLED` (`diarization.rs:30-52`) into `batch/guard.rs`, keeping the `compare_exchange` acquire and the `Drop` release byte-for-byte; verify: standing verification and a grep showing no second definition of either static.
- [ ] 1.12 Move `StageTimings`, `run_diarization_blocking`, `run_diarization_blocking_with_app`, `ChannelSplit`, `channel_split_for_layout`, `run_channel_diarization`, `diarize_decoded_channels`, `run_channel_diarization_stream`, `count_unique_speakers` (`diarization.rs:827-1148`, `2284-2344`) into `batch/orchestrator.rs` with `unknown_layout_never_selects_the_downmix_path`; verify: standing verification.
- [ ] 1.13 Move `group_cluster_embeddings`, `persist_channel_clusters`, `persist_and_recognize_session` (`diarization.rs:2345-2480`) into `persist/clusters.rs`; verify: standing verification.
- [ ] 1.14 Move `compute_speaker_matches` and `find_best_speaker` (`diarization.rs:2481-2594`) into `core/timeline.rs`; verify: standing verification.
- [ ] 1.15 Move `emit_progress` and `MemorySampler` (`diarization.rs:2595-2689`) into `telemetry.rs`; verify: standing verification.
- [ ] 1.16 Move the `#[tauri::command]` functions `get_diarization_status`, `update_speaker_label_command`, `rematch_meeting_speakers`, `set_diarization_clustering_settings`, `check_diarization_models` plus `DiarizationModelStatus`, `cleanup_legacy_models`, `is_diarization_in_progress`, `cancel_diarization` into `commands.rs`, leaving `start_diarization` as the only item left in `legacy.rs`; verify: standing verification and `lib.rs:846-847` unchanged.
- [ ] 1.17 Move `start_diarization` and `refine_offline_rows` (`diarization.rs:204-567`) into `commands.rs` (thin wrapper) plus `batch/orchestrator.rs`, and delete the now-empty `legacy.rs` and its `mod` declaration; verify: standing verification and `audio/diarization.rs` no longer exists.
- [ ] 1.18 Move `audio/speaker_recognition.rs` (282 lines) verbatim to `identity/matching.rs`, keeping `RECOGNITION_THRESHOLD`, `Prototype`, `MatchResult`, `l2_normalize*`, `cosine_similarity`, `best_match_with_threshold`, `best_match` and its 10 tests, and re-export the old path from `audio/mod.rs`; verify: standing verification and `cargo check -p meetily` with `database/repositories/speaker.rs` unedited.
- [ ] 1.19 Move `audio/live_diarization_reconcile.rs` (778 lines) verbatim to `streaming/reconcile.rs`, including the `OnceLock` statics at lines 466-467 and `registry`/`reconciler`/`reset_session`/`spawn_cascade`/`close_cascade`, and re-export the old module path; verify: standing verification, plus `audio/transcription/worker.rs:104,598` and `audio/word_alignment/queue.rs` compile unedited.
- [ ] 1.20 Move `OnlineDiarizationGuard` and `ONLINE_DIARIZATION_ACTIVE` (`online_diarization.rs:36-58`) into `streaming/guard.rs` unchanged; verify: standing verification and a grep showing exactly one definition of `ONLINE_DIARIZATION_ACTIVE`.
- [ ] 1.21 Move `DiarizationMode`, `DiarChannel`, `DiarChannelState`, `LastTurn`, `ChannelStatusLine`, `ChannelStats`, `OnlineDiarizationStats`, `OnlineDiarizationStatus`, the `ONLINE_DIARIZATION_STATS` static and `begin_stats`/`current_stats`/`clear_stats`/`record_block_enqueued`/`diar_channel`/`record_stats` (`online_diarization.rs:59-455`) into `telemetry.rs` alongside the batch telemetry, with all 11 `telemetry_*` tests; verify: standing verification.
- [ ] 1.22 Move `SpeakerAssignment`, `SpeakerTurn`, `OnlineClusterEmbeddings` (`online_diarization.rs:457-506`) into `streaming/units.rs`; verify: standing verification.
- [ ] 1.23 Move `PrototypeStore` and `parse_pipeline_id` (`online_diarization.rs:508-678`) into `identity/prototypes.rs` with the `parse_pipeline_id_*`, `channel_and_id_*`, `bind_*`, and `prototype_store_enhanced_tag_is_192d` tests; verify: standing verification and `database/speaker_commands.rs` compiles unedited.
- [ ] 1.24 Move `SpeakerSegment` and `EmbeddingBuffer` (`online_diarization.rs:691-765`) into `core/cluster.rs`; verify: standing verification.
- [ ] 1.25 Move `TimelineMapper`, `FastChannel`, `Engine`, `create_fast_channel` (`online_diarization.rs:766-825`, `1514-1533`) into `streaming/engine.rs`; verify: standing verification.
- [ ] 1.26 Move `OnlineDiarizationProcessor` and `MIN_SEGMENT_SAMPLES` (`online_diarization.rs:826-1475`) into `streaming/processor.rs`, then delete the emptied `audio/online_diarization.rs` and its `mod` declaration, keeping the `pub use` alias in `audio/mod.rs:162-168`; verify: standing verification and `audio/online_diarization.rs` no longer exists.

## 2. Delete the duplicates

- [ ] 2.1 Delete `find_best_speaker` at `online_diarization.rs:1535-1582` and make `streaming/processor.rs::finalize` call `core/timeline.rs::find_best_speaker`, generic over the label type so the `usize` and `i32` call sites both compile; verify: standing verification, plus a new unit test asserting the shared function returns the same speaker for a fixture exercising overlap, single-speaker gap-fill, the 30 s bound, and the unassigned case (this test is the only intentional count change — record the new baseline in this line).
- [ ] 2.2 Delete `cluster_embeddings_by_labels` and `cluster_embeddings_by_overlap` (`online_diarization.rs:1476-1513`) and route both call sites through `persist/clusters.rs::group_cluster_embeddings`; verify: standing verification and a unit test asserting the grouped output for one fixture is unchanged (same cluster count, same per-cluster durations) against a snapshot captured before the deletion.
- [ ] 2.3 Delete `create_enhanced_embedder` (`online_diarization.rs:679-690`) and construct the streaming embedder through `core/factory.rs`, preserving today's pool size of 1 for the streaming path and the "enhanced models are bundled at build time" error text; verify: standing verification and a test asserting the missing-model error message still names the searched path.
- [ ] 2.4 Collapse the three `assign_tokens_to_speakers` call sites (`diarization.rs:357`, `online_diarization.rs:1412`, `live_diarization_reconcile.rs:300`) onto one wrapper in `core/timeline.rs` that returns the token blocks, leaving each caller to render its own output shape (SQL rows, `TranscriptSegment` clones, `LiveBlock`s); verify: standing verification, and `cargo test -p meetily --lib token_assignment` unchanged.
- [ ] 2.5 Move the inline offline token split and its `INSERT OR IGNORE` rows out of `start_diarization` (`diarization.rs:330-470`) into `persist/offline_split.rs`, called by the batch orchestrator; verify: standing verification and `cargo check -p meetily`, with the emitted SQL statement text unchanged (compare the statement string before/after).

## 3. One parameter surface

- [ ] 3.1 Add `DiarizationConfig` (resolved once) to `OnlineDiarizationProcessor`'s construction, threading it from `DiarizationEngine::start_live_session` so a mid-recording settings change cannot split a session; verify: standing verification and a unit test asserting the processor holds the config captured at construction after `set_clustering_overrides` is called afterwards.
- [ ] 3.2 Replace the hardcoded `crate::audio::embedder::TITANET_CLUSTER_THRESHOLD` at `online_diarization.rs:1283` with the session config's resolved merge threshold and clusterer kind; verify: standing verification, plus a test asserting that with no stored overrides the resolved threshold equals `TITANET_CLUSTER_THRESHOLD` (default equivalence) and that a stored override changes it.
- [ ] 3.3 Enforce the speaker-count ceiling on the live path: resolve `max_speakers` through the same "user value when smaller, else configured ceiling, clamped to 255, minimum 1" rule the batch path uses (`diarization.rs:1250-1253`), so the unset case no longer reaches the clusterer as `0` (`recording_commands.rs:672-673`); verify: standing verification and a unit test asserting an unset user maximum resolves to the configured default ceiling and an oversized one clamps to 255 with a log line.
- [ ] 3.4 Live A/B on one recording: diarize the same stored recording through a live Efficient session before and after section 3 with no stored overrides; verify: the cluster count and the per-transcript speaker labels are identical, and `uv run --project eval subset` from `eval/` still passes its recorded gates (offline path untouched).

## 4. `trait Clustering`

- [ ] 4.1 Add `trait Clustering { fn cluster(&self, embeddings: &[Vec<f32>]) -> Result<Vec<usize>, String>; }` in `core/cluster.rs` with a `GlobalAhc` implementation wrapping what `V2Core::finish` builds today (`diarization.rs:1679-1697`) and a factory taking `(&DiarizationConfig, effective_ceiling)`; verify: standing verification and `cargo test -p meetily --lib clusterer` unchanged.
- [ ] 4.2 Re-implement `EmbeddingBuffer::cluster` on the trait as `BufferedAhc`, keeping the `MinClusterSizeClusterer(.., 2)` singleton dissolution and the single-entry short circuit; verify: standing verification and a test asserting a two-embedding fixture that produced one cluster before produces one cluster after, and a singleton is still reassigned to the nearest larger cluster.
- [ ] 4.3 Make the batch path construct its clusterer through the same factory, so `build_clusterer` has exactly one caller path; verify: standing verification and `uv run --project eval run --dataset voxconverse-dev && uv run --project eval score --dataset voxconverse-dev` reports the same DER as before the change (offline behavior held constant).

## 5. `DiarizationEngine` facade

- [ ] 5.1 Create `engine.rs` with the `DiarizationEngine` unit struct and the input/output types from design D2 (`LiveSessionConfig`, `SessionOutcome`, `PersistSummary`, `LiveSpeakerAssignment`, `BatchRequest`), and move the session statics `ONLINE_DIARIZATION_TASK`, `ONLINE_DIARIZATION_STORE`, `ONLINE_SESSION_DATA`, `ONLINE_TURN_OVERRIDES`, `ONLINE_EXPECTED_SPEAKER_IDS` (`recording_commands.rs:53,59,78,99`) into it as private items; verify: standing verification and a grep showing no `ONLINE_*` static declared outside `engine.rs`.
- [ ] 5.2 Implement `start_live_session` by moving the live-session block of `start_recording_with_devices_and_meeting` (`recording_commands.rs:600-707`: stats reset, `reset_session`, prototype-store load, processor construction, `spawn_blocking` drain loop) into the engine, leaving the caller with one call; verify: standing verification and a Fast-mode recording smoke run that still emits `online-speaker-turn` and `live-transcript-blocks` events.
- [ ] 5.3 Implement `finalize_session` by moving the stop-time block (`recording_commands.rs:1052-1155`: task join, stop-time token repair, `processor.finalize`, session-data hand-off) into the engine; verify: standing verification and a stop of a Fast-mode recording that still produces the same number of speaker assignments as before for the same recording.
- [ ] 5.4 Implement `persist_session` by moving the body of `finalize_online_session` (`recording_commands.rs:1803-2043`) into the engine, leaving the `#[tauri::command]` as a wrapper; verify: standing verification and a live-rename recording whose reopened meeting still shows the user's name with user provenance.
- [ ] 5.5 Implement `assign_live_speaker` by moving the body of the command (`recording_commands.rs:2185-2249`) into the engine, preserving the loud failure when no live prototype store is active; verify: standing verification and a unit test asserting the no-store case returns an error rather than `Ok`.
- [ ] 5.6 Implement `telemetry_snapshot` by moving `online_diarization_status` (`recording_commands.rs:2045-2117`) into the engine, leaving `get_recording_telemetry` as the command; verify: standing verification and the telemetry panel showing the same values for a running session.
- [ ] 5.7 Add `live_prototype_store()` and replace the direct static access in `database/speaker_commands.rs:562-566`, keeping its non-blocking `try_lock` semantics so a speaker command never blocks on a live session; verify: standing verification and `cargo check -p meetily` with no remaining reference to `recording_commands::ONLINE_DIARIZATION_STORE`.
- [ ] 5.8 Add `start_batch`, `cancel_batch`, `is_batch_running`, and `set_clustering_settings` as the facade entries over `commands.rs`/`config.rs`, so change 07 has one symbol to call for offline diarization; verify: standing verification and offline diarization of a saved meeting still completing with the same speaker labels.

## 6. Wrap-up

- [ ] 6.1 Refresh the module documentation comment at the top of `audio/diarization/mod.rs` to state the tree layout and that `DiarizationEngine` is the only entry point other modules may use; verify: the comment names every submodule that exists.
- [ ] 6.2 Run `cargo test -p meetily --lib`, `cargo clippy -p meetily --all-targets --message-format=short`, and `cargo check -p meetily --bins`; verify: the whole-lib passing count equals the 1.1 baseline plus exactly the tests added in 2.1, 2.2, 2.3, 3.1, 3.2, 3.3, 4.2, and 5.5, and no new clippy warning.
- [ ] 6.3 Run `openspec validate 05-unified-diarization-engine --strict` and `openspec status --change 05-unified-diarization-engine`; verify: validation passes and every artifact is reported done.
