## 1. Grouping helper

- [x] 1.1 Create `frontend/src/lib/speaker-turn-grouping.ts` with a pure `groupIntoTurns(transcripts)` that merges chronologically adjacent segments sharing the same resolved speaker + `source_device`, breaks on speaker or channel change, on a missing speaker, and on a gap >= 60s; turn carries `id: turn:<first-segment-id>`, `start`/`end` spanning members, and the member list. Verify: unit tests in `frontend/tests/lib/speaker-turn-grouping.test.ts` cover merge, speaker-change split, channel-change split, unresolved-speaker isolation, gap break, and span aggregation (mirrors the `live-speaker-labels.test.ts` setup).
- [x] 1.2 Verify helper does not sort or mutate input: write a test that passes an unsorted array and asserts the input reference/equality is unchanged (sorting stays upstream).

## 2. Meeting-details view integration

- [x] 2.1 In the meeting-details transcript flow (`TranscriptContext` -> `TranscriptPanel` -> `VirtualizedTranscriptView`), consume grouping: map persisted transcripts through `groupIntoTurns` for the default chat-style rendering. Verify: a meeting with several consecutive same-speaker records shows one turn block; a speaker change shows separate blocks.
- [x] 2.2 Adapt merged-turn rendering in `VirtualizedTranscriptView`: one header (speaker dot/label, play button, turn timestamp), concatenated member texts, per-member inline editing and split entry points preserved. Verify: editing a member text still uses the member record; split action still appears for members.
- [x] 2.3 Change the active-play highlight from `activeSegmentId === segment.id` to turn-window containment (`isAudioPlaying` && playback time within turn `[start, end]`) for merged turns and keep the play glyph reversion behavior (clip-playback-indicator). Verify: play a member segment; the turn is highlighted and the button toggles play/pause as before (see `split-transcript-ui` playing-block requirement).

## 3. Live view integration

- [x] 3.1 In the live recording view, recompute grouped turns in a `useMemo` over the resolved transcript list (the same inputs `rematchTranscripts` consumes), and pass grouped turns to the same renderer path. Verify: during a live recording with one speaker pausing > 500ms between utterances, consecutive records render inside one turn; a second speaker's utterance starts a new turn.
- [x] 3.2 Verify late speaker events re-form the merge: simulate a `transcript-update`/speaker-turn event that changes a segment's resolved speaker and assert the display splits/re-merges accordingly (unit-level around `rematchTranscripts` output + `groupIntoTurns`).
- [x] 3.3 Verify pinned/user-assigned labels surface on merged turns unchanged: with a pinned live label (live-speaker-labels logic), the merged turn shows the pinned name and rename/edit of a member still works.

## 4. Word rows and modes

- [x] 4.1 Render live word-level sub-rows (`upsertLiveBlocks`/`resolveLiveBlocks`) inside each member container of a merged turn without changing their `parent_sequence_id` keying. Verify: word-alignment sub-rows appear per member inside the merged turn and re-attribution replaces rows as before.
- [x] 4.2 Confirm the existing collapsible grouping mode still works with merged turns nested in speaker sections. Verify: toggle collapsible grouping; sections remain collapsed/expansible and contain merged turn rows. *(Finding: no collapsible transcript grouping mode exists in the current codebase — the views (`VirtualizedTranscriptView`, both `TranscriptPanel`s) render only the flat chat-style layout, and the standalone `TranscriptView.tsx` is unused. Nothing to confirm against; grouping affects no other mode.)*
- [x] 4.3 Render a merged member's live word-level sub-rows on the member's source side: the sub-row container, label order and timestamp side follow the turn's/member's `source_device` instead of the current hard-coded left layout, with the sub-row data path (`upsertLiveBlocks`/`resolveLiveBlocks`, `parent_sequence_id` keying) untouched. Verify: automated check of the extracted side rule; manual confirmation that a System turn whose member was split into several short word runs shows every sub-row on the System side and none on the Microphone side (and the mirrored case for a Microphone turn). *(Side rule extracted as `sourceSideLayout(sourceDevice)` in `frontend/src/lib/speaker-turn-grouping.ts` and used for the turn header, each member's sub-row container, and member text; 4 new unit cases in `frontend/tests/lib/speaker-turn-grouping.test.ts` pass under the Node strip-types runner. Manual confirmation is tracked by 5.2.)*

## 5. End-to-end validation

- [x] 5.1 Full pass: run frontend unit tests (`frontend/tests`) including new `speaker-turn-grouping.test.ts` and existing `live-speaker-labels.test.ts`, lint, and typecheck; verify all pass. *(bun is not installed on this machine; the new helper, `withTurnGroups`, and the live-integration semantics (late event split / pinned label / isolation) were verified with Node strip-types runners replicating the same assertions. `npx tsc --noEmit` is clean for changed files — only pre-existing `bun:test` module errors remain; `npm run lint` reports only pre-existing issues.)*
- [ ] 5.2 Manual check both views (live recording with a single speaker who pauses, then an imported/historical meeting): readable turn blocks, correct first-segment start seek on play, no changes to saved `transcripts.json` content between before/after a recording stop, and no split member rendered on the opposite side of its turn (a System member split by live word-level diarization stays right-aligned, a Microphone member stays left-aligned).
