## 1. Revert the grouping render integration

- [x] 1.1 Restore the four code files that carry the grouping changes (`frontend/src/app/_components/TranscriptPanel.tsx`, `frontend/src/components/MeetingDetails/TranscriptPanel.tsx`, `frontend/src/components/VirtualizedTranscriptView.tsx`, `frontend/src/types/index.ts`) to their committed state, since their uncommitted diffs are grouping-only (`withTurnGroups` calls, the `turnMembers` field, the merged-turn branch, the `sourceSideLayout` usage). Verify: `git status --short` no longer lists any of the four paths as modified, and `grep -n "turnMembers\|withTurnGroups"` across them returns nothing. *(Done with `git restore --source=HEAD --worktree`; diffs were re-checked as grouping-only beforehand (only the `withTurnGroups(` wrap and its indentation, plus the `turnMembers` field). Both checks pass.)*
- [x] 1.2 Delete the grouping helper and its tests: `frontend/src/lib/speaker-turn-grouping.ts` and `frontend/tests/lib/speaker-turn-grouping.test.ts`. Verify: `grep -rn "speaker-turn-grouping\|withTurnGroups\|groupIntoTurns\|identityKey\|sourceSideLayout" frontend/src frontend/tests` returns nothing, and `git status --short` shows the two paths gone. *(Both deleted; the repository-wide grep across `frontend/src` and `frontend/tests` returns nothing.)*

## 2. Verification

- [x] 2.1 Typecheck and lint the reverted frontend: `npx tsc --noEmit` and `npx eslint src/components/VirtualizedTranscriptView.tsx src/app/_components/TranscriptPanel.tsx src/components/MeetingDetails/TranscriptPanel.tsx`. Verify: only the pre-existing diagnostics remain (`bun:test` module-resolution errors in `frontend/tests`, the unused `useCallback` import and the two unescaped quotes at `VirtualizedTranscriptView.tsx:399`); no new errors. *(Run: `tsc` reports only four `bun:test` resolution errors — one fewer than before, matching the deleted test file; eslint reports the same pre-existing findings in `VirtualizedTranscriptView.tsx` (unused `useCallback`, two unescaped quotes, now at line 398 after the revert) and, when `src/types/index.ts` is included, four `no-explicit-any` errors that already exist at HEAD.)*
- [ ] 2.2 Manual check in both views: start a live recording where one speaker pauses for more than 500 ms and a second speaker talks, then open a historical meeting. Verify each transcript record renders as its own row (no merged turn, no expand/collapse control), the microphone/System/legacy variants keep their side, bubble, speaker label and play button, word-level split sub-rows still render, and `transcripts.json` is unchanged by viewing.

## 3. Retire the superseded changes

- [x] 3.1 Retire `speaker-turn-display-grouping` and `identity-turn-grouping` with `openspec archive <name> --yes --skip-specs`, so their deltas never reach the main specs and the decision record stays under `openspec/changes/archive/`. Verify: `openspec list --json` no longer reports either change, both directories exist under `openspec/changes/archive/`, and `openspec list --specs --json` has no `speaker-turn-grouping` entry. *(Done by the user directly: both commands were run once the directory-handle lock was released, after the earlier `EPERM` staging failures. Verified: active changes are `drop-turn-grouping`, `live-word-level-diarization`, `tags-persistence-and-palette`, `add-online-diarization-eval`; archive holds `2026-09-18-speaker-turn-display-grouping` and `2026-09-18-identity-turn-grouping`; no `speaker-turn-grouping` capability in the main specs.)*
