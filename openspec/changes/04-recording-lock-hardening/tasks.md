# Tasks

## 1. Poison-safe lock helper

- [ ] 1.1 Create `frontend/src-tauri/src/audio/sync_ext.rs` with a `LockRecover<T>` trait implemented for `std::sync::Mutex<T>`, exposing `lock_or_recover(&self) -> MutexGuard<'_, T>` that logs `log::error!` and calls `poisoned.into_inner()` on `Err`; register `pub mod sync_ext;` in `frontend/src-tauri/src/audio/mod.rs`; verify with `cargo check -p meetily`.
- [ ] 1.2 Add a unit test in `sync_ext.rs` that wraps a `Mutex<i32>` in an `Arc`, spawns a scoped thread that locks it and panics while holding the guard, joins that thread (ignoring the panic via `catch_unwind` or a plain `let _ = handle.join()`), then calls `lock_or_recover()` from the test thread and asserts it returns successfully with the value the panicking thread last set; verify with `cargo test -p meetily --lib sync_ext::`.
- [ ] 1.3 Replace every `.lock().unwrap()` in `frontend/src-tauri/src/audio/recording_commands.rs` (grep count before/after: `grep -c "\.lock()\.unwrap()" frontend/src-tauri/src/audio/recording_commands.rs` must go from 40 to 0) with `.lock_or_recover()`, adding `use super::sync_ext::LockRecover;`; verify with `cargo check -p meetily` and the grep count.
- [ ] 1.4 Replace every `.lock().unwrap()` in `frontend/src-tauri/src/audio/recording_state.rs` (grep count 38 → 0) with `.lock_or_recover()`; verify with `cargo check -p meetily` and `grep -c "\.lock()\.unwrap()" frontend/src-tauri/src/audio/recording_state.rs`.
- [ ] 1.5 Replace both `.lock().unwrap()` sites in `frontend/src-tauri/src/audio/live_diarization_reconcile.rs` (lines 115 and 210, and any others found by `grep -n "\.lock()\.unwrap()" frontend/src-tauri/src/audio/live_diarization_reconcile.rs`) with `.lock_or_recover()`; verify with `cargo check -p meetily`.
- [ ] 1.6 Run the full existing test suite to confirm no behavior regression from the trait swap; verify with `cargo test -p meetily --lib`.

## 2. Fix `attempt_device_reconnect` lock-across-await

- [ ] 2.1 Rewrite `attempt_device_reconnect` (`frontend/src-tauri/src/audio/recording_commands.rs:1742-1801`) to take the `RecordingManager` out of `RECORDING_MANAGER` under a lock, drop the lock, call `.await` on the owned manager directly (removing the `tokio::task::spawn_blocking` + `Handle::current().block_on` wrapper), then re-lock and restore the manager (mirroring `stop_recording`'s Step 1 pattern at recording_commands.rs:927-930); verify with `cargo check -p meetily` and by confirming no `spawn_blocking`/`block_on` remain in the function body (`grep -n "spawn_blocking\|block_on" frontend/src-tauri/src/audio/recording_commands.rs` shows no match inside `attempt_device_reconnect`'s line range).
- [ ] 2.2 Add a unit test (in `recording_commands.rs` or a new `#[cfg(test)]` module) that: puts a `RecordingManager` stand-in behind `RECORDING_MANAGER` (or, if `RecordingManager` cannot be constructed cheaply in a unit test, add a focused test at the `RECORDING_MANAGER`-guard level using a local `static`/helper mirroring the same take-drop-restore shape) simulating a reconnect that sleeps for e.g. 200ms before returning, and asserts that a concurrent attempt to lock and read the same `Mutex` (simulating `stop_recording`'s guard acquisition) succeeds in well under the 200ms sleep duration (e.g. under 20ms), proving the lock is not held for the sleep's duration; verify with `cargo test -p meetily --lib attempt_device_reconnect` (or the chosen test name) and confirm it fails against the pre-fix code path if run before task 2.1 (write the test, temporarily check it fails on the old code by stashing the fix, then restore).
- [ ] 2.3 Manually confirm the return type and error strings of `attempt_device_reconnect` are unchanged (still `Result<bool, String>`, `"Recording not active"` on no active manager, `"Task join error: ..."` removed since `spawn_blocking` is gone — update the frontend caller if it pattern-matches that specific string; check with `grep -rn "Task join error" frontend/src`); verify with `cargo check -p meetily` and a manual diff read.

## 3. Explicit stop signals for background threads

- [ ] 3.1 Change the ffmpeg stderr reader thread (`frontend/src-tauri/src/audio/diarization.rs:2203`) from a single blocking `stderr.read_to_end(&mut buf)` to a loop with bounded reads (`stderr.read(&mut chunk)` on a fixed-size buffer) that checks a shared `Arc<AtomicBool>` stop flag each iteration and breaks on `Ok(0)` (pipe closed), a read error, or the stop flag; store the `Arc<AtomicBool>` and `JoinHandle` on `PcmStream` (or the owning struct) and add `fn stop(&mut self, timeout: Duration) -> bool` that sets the flag and polls `JoinHandle::is_finished()` in a short sleep loop until `timeout` elapses, returning whether it joined in time; verify with `cargo check -p meetily`.
- [ ] 3.2 Add a unit test that starts the stderr-reader thread against a real child process (or a mock pipe/`std::io::Cursor`-backed reader if the thread's `read` source can be made generic for testing) that would otherwise block, calls `stop()` with a short timeout, and asserts it returns `true` (joined) within the timeout; verify with `cargo test -p meetily --lib` filtered to the new test name.
- [ ] 3.3 Add `pub fn stop(&mut self, timeout: Duration) -> bool` to `MemorySampler` (`frontend/src-tauri/src/audio/diarization.rs:2611-2637`) that sets `self.running` to `false` and polls `self.handle.take()`'s `is_finished()` with the same timed-join helper as 3.1, returning whether it joined in time; change `Drop` (line 2621) to call this new `stop()` with a fixed generous timeout (e.g. 1s) instead of an untimed `handle.join()`; verify with `cargo check -p meetily`.
- [ ] 3.4 Add a unit test that starts a `MemorySampler`, immediately calls `stop()` with a short timeout, and asserts it returns `true` promptly (well under the sampler's polling interval multiplied by a few iterations); verify with `cargo test -p meetily --lib` filtered to the new test name.

## 4. Bounded channels for audio-carrying paths

- [ ] 4.1 Change `pipeline.rs:1303`'s `mpsc::unbounded_channel::<AudioChunk>()` to `mpsc::channel::<AudioChunk>(128)`; update `RecordingState::send_audio_chunk` (`recording_state.rs:276-291`) to use `try_send` and, on `Err(TrySendError::Full(_))`, log `warn!("audio_sender channel full ({} capacity); dropping chunk {}", 128, chunk.chunk_id)` and return `Ok(())` (treated the same as the existing paused-silently-discard path) instead of propagating an error; verify with `cargo check -p meetily`.
- [ ] 4.2 Change `recording_manager.rs:75`'s `mpsc::unbounded_channel::<AudioChunk>()` to `mpsc::channel::<AudioChunk>(32)`; update `AudioPipeline::flush_pending_segments`'s `self.transcription_sender.send(...)` (`pipeline.rs:908`) to `try_send`, logging `warn!` and dropping on `Full` instead of the current `warn!`-and-continue on any `Err` (keep the existing closed-channel handling, add the `Full` case explicitly); verify with `cargo check -p meetily`.
- [ ] 4.3 Change `recording_commands.rs:607`'s `mpsc::unbounded_channel::<AudioChunk>()` (the embedding channel) to `mpsc::channel::<AudioChunk>(32)`; update `embedding_sender.send(...)` (`pipeline.rs:919`) to `try_send` with the same drop-and-log-on-`Full` policy, keeping the existing `record_block_enqueued()` call only on a successful send (move it after the `try_send` succeeds, or add a corresponding "dropped" counter call if one exists — check `crate::audio::online_diarization` for a matching decrement/drop function and use it if present); verify with `cargo check -p meetily`.
- [ ] 4.4 Change `recording_saver.rs:182`'s `mpsc::unbounded_channel::<AudioChunk>()` to `mpsc::channel::<AudioChunk>(128)`; update its sender's call site(s) (find with `grep -n "\.send(" frontend/src-tauri/src/audio/recording_saver.rs` and the producer that holds this channel's `Sender`) to `try_send` with drop-and-log-on-`Full`; verify with `cargo check -p meetily`.
- [ ] 4.5 Add a unit test for the drop policy: construct a `mpsc::channel::<T>(1)` (or reuse one of the four production channels' element type with a minimal fixture), fill it with `try_send`, `try_send` a second item and assert it returns `Err(TrySendError::Full(_))`, then `recv()` the first item and assert a subsequent `try_send` succeeds — proving the chosen capacity-1 case behaves as documented; verify with `cargo test -p meetily --lib` filtered to the new test name.
- [ ] 4.6 Confirm by reading (no code change) that `recording_commands.rs:623`, `post_processor.rs:36-37`, `batch_processor.rs:29`, `device_monitor.rs:90`, and `async_logger.rs:25` remain `mpsc::unbounded_channel`; verify with `grep -n "unbounded_channel" frontend/src-tauri/src/audio/recording_commands.rs frontend/src-tauri/src/audio/post_processor.rs frontend/src-tauri/src/audio/batch_processor.rs frontend/src-tauri/src/audio/device_monitor.rs frontend/src-tauri/src/audio/async_logger.rs` showing exactly these five files/lines unchanged.

## 5. Verification

- [ ] 5.1 Run `cargo clippy -p meetily --all-targets` and confirm no new warnings/errors are introduced beyond the pre-existing ~223 baseline (compare warning count before/after this change's diff); verify by inspecting the clippy output diff.
- [ ] 5.2 Run `cargo test -p meetily --lib` and confirm all tests pass, including the new tests from tasks 1.2, 2.2, 3.2, 3.4, 4.5; verify with the command's exit code and output.
- [ ] 5.3 Run `openspec validate 04-recording-lock-hardening --strict` and fix any reported issues; verify with the command's exit code and "Change is valid" output.
