# Tasks

## 1. IPC core

- [ ] 1.1 Create `frontend/src/lib/ipc/core.ts` exporting `IpcError` (extends `Error`, carries `command: string` and `cause: unknown`), `normalizeMessage(reason: unknown): string` (string passthrough, `Error.message`, else `String(reason)`), and `invokeTyped<TArgs, TResult>(cmd, args?, opts?: { debug?: boolean })` that calls `invoke` from `@tauri-apps/api/core` and rethrows failures as `IpcError`; verify with `cd frontend && npx tsc --noEmit -p .`.
- [ ] 1.2 Add `listenTyped<TPayload>(event, handler)` to `core.ts`, wrapping `listen` from `@tauri-apps/api/event` with the same `TPayload` generic and no behavior change (still returns `Promise<UnlistenFn>`); verify with `npx tsc --noEmit -p .`.
- [ ] 1.3 Add a module-level debug switch (`setIpcDebug(enabled: boolean)` / internal flag, default `false`) that gates a `console.debug('[ipc]', cmd, ...)` log on success and failure in `invokeTyped`; verify by reading the diff (no call site needs to pass `debug` explicitly for existing behavior to hold).
- [ ] 1.4 Add `frontend/tests/lib/ipc/core.test.ts` using `bun:test`'s `mock.module('@tauri-apps/api/core', () => ({ invoke: mock(...) }))` to assert: a rejected string reason becomes `IpcError` with that message and the right `command`; a rejected `Error` keeps its `.message`; a successful call resolves with the mocked value unchanged; verify with `npx bun test tests/lib/ipc/core.test.ts`.

## 2. Domain modules

- [ ] 2.1 Create `frontend/src/lib/ipc/recording.ts` covering `start_recording`, `start_recording_with_devices_and_meeting`, `stop_recording`, `pause_recording`, `resume_recording`, `is_recording`, `get_audio_devices`, `set_audio_backend`, `set_recording_preferences`, `get_expected_speakers`, `set_expected_speakers`, `start_audio_level_monitoring`, `stop_audio_level_monitoring`, plus `listenRecordingStarted`/`listenRecordingPaused`/`listenRecordingResumed`/`listenSpeechDetected` (moving the event names currently in `services/recordingService.ts:156,176,185,205`); verify with `npx tsc --noEmit -p .`.
- [ ] 2.2 Create `frontend/src/lib/ipc/transcript.ts` covering `verify_meeting_caches`, `purge_unconfirmed_caches`, `start_retranscription_command`, plus `listenTranscriptionComplete`/`listenTranscriptError`/`listenTranscriptionError` (event names from `services/transcriptService.ts:68`, `components/RecordingControls.tsx:258,280`, `hooks/useRecordingStop.ts:179`); import `Transcript`/`TranscriptUpdate` types from `frontend/src/types/index.ts` rather than redeclaring them; verify with `npx tsc --noEmit -p .`.
- [ ] 2.3 Create `frontend/src/lib/ipc/speakers.ts` covering `assign_speaker`, `assign_live_speaker`, `verify_speaker`, `rename_speaker`, `list_speakers`, `speaker_storage_stats`, `rematch_meeting_speakers`, `reconfirm_voiceprint`, `verify_voiceprint`, `clear_all_voiceprints`; verify with `npx tsc --noEmit -p .`.
- [ ] 2.4 Create `frontend/src/lib/ipc/models.ts` covering the whisper (`whisper_init`, `whisper_load_model`, `whisper_get_available_models`, `whisper_get_current_model`, `whisper_get_models_directory`, `whisper_has_available_models`, `whisper_is_model_loaded`, `whisper_validate_model_ready`, `whisper_download_model`, `whisper_cancel_download`, `whisper_delete_corrupted_model`, `whisper_transcribe_audio`), parakeet (`parakeet_init`, `parakeet_get_available_models`, `parakeet_get_current_model`, `parakeet_get_models_directory`, `parakeet_has_available_models`, `parakeet_is_model_loaded`, `parakeet_validate_model_ready`, `parakeet_load_model`, `parakeet_download_model`, `parakeet_retry_download`, `parakeet_transcribe_audio`), builtin-ai (`builtin_ai_list_models`, `builtin_ai_download_model`, `builtin_ai_cancel_download`, `builtin_ai_delete_model`, `builtin_ai_get_models_directory`), and hosted-provider listing (`get_ollama_models`, `pull_ollama_model`, `get_anthropic_models`, `get_groq_models`, `get_openai_models`, `get_openrouter_models`) commands, plus `listenBuiltinAiDownloadProgress` (event from `components/BuiltInModelManager.tsx:81`); keep the `builtin_ai_get_models_directory` wrapper (it has no current caller and no matching Rust command — see design.md D-none/Open Questions) but do not call it from any new code; verify with `npx tsc --noEmit -p .`.
- [ ] 2.5 Create `frontend/src/lib/ipc/summary.ts` covering `api_get_summary`, importing `Summary`/`SummaryDataResponse` from `types/index.ts`; verify with `npx tsc --noEmit -p .`.
- [ ] 2.6 Create `frontend/src/lib/ipc/settings.ts` covering `api_get_model_config`, `api_save_model_config`, `api_get_api_key`, `api_get_custom_openai_config`, `api_save_custom_openai_config`, `api_get_transcript_config`, `api_save_transcript_config`, `api_get_transcript_api_key`, `set_language_preference`, `set_notification_settings`, `set_word_alignment_settings`, `open_system_settings`, `open_models_folder`, `open_recordings_folder`, `open_external_url`, `toggle_console`, `show_console`, `hide_console`, and the known-dangling `api_get_auto_generate_setting` (typed but documented with a `@remarks` note that the command is not currently registered — see design.md Open Questions); verify with `npx tsc --noEmit -p .`.
- [ ] 2.7 Create `frontend/src/lib/ipc/meetings.ts` covering `api_get_meetings`, `api_delete_meeting`, `api_save_meeting_title`, `api_search_transcripts`, `assign_tag`, `unassign_tag`, `create_and_assign_tag`, `delete_tag`, `set_tag_color`, `import_and_initialize_database`, `initialize_fresh_database`; verify with `npx tsc --noEmit -p .`.
- [ ] 2.8 Create `frontend/src/lib/ipc/analytics.ts` covering `init_analytics`, `start_analytics_session`, `end_analytics_session`, `is_analytics_enabled`, `is_analytics_session_active`, `disable_analytics`, `identify_user`, and every `track_*` command called today (`track_event`, `track_analytics_enabled`, `track_analytics_disabled`, `track_analytics_transparency_viewed`, `track_custom_prompt_used`, `track_daily_active_user`, `track_feature_used`, `track_meeting_deleted`, `track_meeting_started`, `track_model_changed`, `track_recording_started`, `track_recording_stopped`, `track_settings_changed`, `track_summary_generation_completed`, `track_summary_regenerated`, `track_user_first_launch`); verify with `npx tsc --noEmit -p .`.
- [ ] 2.9 Add `frontend/tests/lib/ipc/<one-domain>.test.ts` (recommended: `analytics.test.ts`, the module with the most commands) mocking `@tauri-apps/api/core`'s `invoke` and asserting one typed function calls it with the right command name and args and returns the mocked value; verify with `npx bun test tests/lib/ipc/`.

## 3. Enforce the boundary

- [ ] 3.1 Add `rules.no-restricted-imports` for `@tauri-apps/api/core` and `@tauri-apps/api/event` to `frontend/.eslintrc.json`, with an `overrides` entry re-enabling both imports under `src/lib/ipc/**/*.ts` and in `src/app/layout.tsx` (the two generic Tauri window-drag events and the app-level custom event stay there per design.md D2); verify with `cd frontend && npx next lint` showing no `no-restricted-imports` violations outside the allow-list.

## 4. Migrate call sites — components first

- [ ] 4.1 Migrate `frontend/src/components/Sidebar/index.tsx` (7 invoke calls) and `frontend/src/components/Sidebar/SidebarProvider.tsx` (3) to `lib/ipc/*`; verify with `npx tsc --noEmit -p .` and `npx next lint` (no `no-restricted-imports` violation in these files).
- [ ] 4.2 Migrate `frontend/src/components/MeetingTags/TagEditorPopover.tsx` (6 invoke calls) to `lib/ipc/meetings.ts`; verify with `npx tsc --noEmit -p .`.
- [ ] 4.3 Migrate `frontend/src/components/VoiceprintBrowser.tsx` (5 invoke calls) to `lib/ipc/speakers.ts`; verify with `npx tsc --noEmit -p .`.
- [ ] 4.4 Migrate `frontend/src/components/ModelSettingsModal.tsx` (16 invoke calls, including the dangling `api_get_auto_generate_setting` at line 320) to `lib/ipc/settings.ts` and `lib/ipc/models.ts`; verify with `npx tsc --noEmit -p .` and a manual smoke check that provider switching still loads cached models (no functional change expected).
- [ ] 4.5 Migrate the remaining component files with direct `invoke`/`listen` calls (`OnboardingContext.tsx` 9, `builtin-ai.ts` callers via `BuiltInModelManager.tsx` 4 + `listen`, `SummaryModelSettings.tsx` 4, `RecordingControls.tsx` 4 + 3 listen, `BuiltInModelManager.tsx` 4, `AnalyticsConsentSwitch.tsx` 4, `app/meeting-details/page.tsx` 4, `alignmentService.ts` 3, `ConfigContext.tsx` 3, `onboarding/steps/DownloadProgressStep.tsx` 3, `PreferenceSettings.tsx` 3, `ConsoleToggle.tsx` 3, and the 12 files with 1-2 calls each); verify with `npx tsc --noEmit -p .` and `npx next lint`.

## 5. Migrate call sites — services and lib

- [ ] 5.1 Migrate `frontend/src/services/recordingService.ts` (22 invoke + 4 listen) to call `lib/ipc/recording.ts` internally, keeping its exported class/method surface unchanged so its callers need no edits; verify with `npx tsc --noEmit -p .` and `npx bun test tests/` (existing passing tests must stay passing; the 2 pre-existing failures noted in change 02 are out of scope here).
- [ ] 5.2 Migrate `frontend/src/lib/analytics.ts` (23 invoke calls) to `lib/ipc/analytics.ts`, keeping its exported `Analytics` API unchanged; verify with `npx tsc --noEmit -p .`.
- [ ] 5.3 Migrate `frontend/src/lib/whisper.ts` and `frontend/src/lib/parakeet.ts` (13 invoke calls each) to `lib/ipc/models.ts`, keeping their exported class APIs unchanged; verify with `npx tsc --noEmit -p .`.
- [ ] 5.4 Migrate `frontend/src/lib/builtin-ai.ts` (8 invoke calls) to `lib/ipc/models.ts`; verify with `npx tsc --noEmit -p .`.
- [ ] 5.5 Re-point `frontend/src/services/configService.ts`, `frontend/src/services/diarizationStatusService.ts`, `frontend/src/services/transcriptService.ts`, `frontend/src/services/alignmentService.ts` to call the matching `lib/ipc/*` function internally instead of `invoke` directly, with no change to their exported types/methods; verify with `npx tsc --noEmit -p .`.
- [ ] 5.6 Migrate the remaining `hooks/*` single-call sites (`useImportAudio.ts` 2 + listen, `useTranscriptRecovery.ts` 1, `usePermissionCheck.ts` 1) to `lib/ipc/*`; verify with `npx tsc --noEmit -p .`.

## 6. Final verification

- [ ] 6.1 Run `grep -rln "@tauri-apps/api/core\|@tauri-apps/api/event" frontend/src --include=*.ts --include=*.tsx | grep -v "lib/ipc/" | grep -v "app/layout.tsx"` and confirm it returns no files; verify by inspecting the (empty) output.
- [ ] 6.2 Run `cd frontend && npx tsc --noEmit -p .`, `npx next lint`, and `npx bun test tests/`; verify all three complete with no new errors (the 2 pre-existing `bun test` failures from change 02's audit are unaffected by this change and remain tracked there).
- [ ] 6.3 Run `openspec validate 08-frontend-typed-ipc-layer --strict`; verify it passes.
