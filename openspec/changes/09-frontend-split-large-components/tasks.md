# Tasks

## 1. ModelSettingsModal split

- [ ] 1.1 Create `frontend/src/hooks/useModelSettings.ts` moving every `useState`/`useRef` from `ModelSettingsModal.tsx:126-176`, the derived flags `requiresApiKey`/`ollamaEndpointChanged`/`isCustomOpenAIInvalid`/`isSaveDisabled` (`:237-256`), and the 15 `useEffect` hooks (`:190-771`) into the hook, returning all of them; verify with `cd frontend && npx tsc --noEmit -p .` (temporarily re-exporting everything from `ModelSettingsModal.tsx` unchanged so behavior is identical before JSX moves).
- [ ] 1.2 Move `handleSave` (`:617`), `testCustomOpenAIConnection` (`:667`), `handleInputClick` (`:694`), and the provider-model loader functions (`:464-604`) into `useModelSettings.ts`, re-pointing their `invoke(...)` calls at `lib/ipc/settings.ts` (`saveCustomOpenAIConfig`, `saveModelConfig`, `getApiKey`) and `lib/ipc/models.ts` (`getAnthropicModels`, `getGroqModels`, `getOpenAIModels`, `getOllamaModels`, builtin-ai listing) from change 08; verify with `npx tsc --noEmit -p .`.
- [ ] 1.3 Create `frontend/src/components/ModelSettings/ApiKeyField.tsx` from `ModelSettingsModal.tsx:1072-1112` (label, masked input, lock/unlock, show/hide) taking `value`, `locked`, `visible`, `vibrating`, `onChange`, `onToggleLock`, `onToggleVisible` as props; verify with `npx tsc --noEmit -p .`.
- [ ] 1.4 Create `frontend/src/components/ModelSettings/providers/HostedProviderFields.tsx` from the model combobox (`:886-940`) plus `<ApiKeyField>`, taking `provider: 'claude' | 'groq' | 'openai' | 'openrouter'`, the provider's model list, loading flag, and the `useModelSettings()` fields it needs, used for all four hosted providers (see design.md D1 for why these four share one file instead of `OpenAI.tsx`/`Anthropic.tsx`/`Groq.tsx`/`OpenRouter.tsx`); verify with `npx tsc --noEmit -p .`.
- [ ] 1.5 Create `frontend/src/components/ModelSettings/providers/Ollama.tsx` from `:1117-1362`, `CustomOpenAI.tsx` from `:948-1071`, and `BuiltIn.tsx` from `:1362-1408`; verify with `npx tsc --noEmit -p .`.
- [ ] 1.6 Create `frontend/src/components/ModelSettings/index.tsx` rendering the provider `Select` (`:876-883`) and switching to `HostedProviderFields`/`Ollama`/`CustomOpenAI`/`BuiltIn` on `modelConfig.provider`, calling `useModelSettings()` for all state; update `frontend/src/components/ModelSettingsModal.tsx` to re-export `ModelSettings/index.tsx`'s component under the existing `ModelSettingsModal` name and `ModelSettingsModalProps`/`ModelConfig` types (no caller changes); verify with `npx tsc --noEmit -p .` and `npx next lint` (no new errors in these files).
- [ ] 1.7 Manual smoke test: for each of the 7 providers (builtin-ai, claude, custom-openai, groq, ollama, openai, openrouter), open the settings modal, switch to that provider, change its model/endpoint/API key, save, reopen the modal, and confirm the saved value reloads; verify by checking off each provider in this task's PR description.

## 2. VoiceprintBrowser split

- [ ] 2.1 Move `PersonPickerDialog` (`VoiceprintBrowser.tsx:70-201`) to `frontend/src/components/Voiceprints/dialogs/PersonPickerDialog.tsx`, `ConfirmReplaceDialog` (`:203-250`) to `ConfirmReplaceDialog.tsx`, `ConfirmClearAllDialog` (`:251-288`) to `ConfirmClearAllDialog.tsx`, `ConfirmPurgeCachesDialog` (`:289-335`) to `ConfirmPurgeCachesDialog.tsx`, each keeping its existing props unchanged; verify with `npx tsc --noEmit -p .`.
- [ ] 2.2 Create `frontend/src/hooks/useVoiceprintActions.ts` moving the state block (`:337-352`), `load` (`:353-367`), the 4 `useEffect` hooks that react to playback (`:369-399`), `toggleGroup`/`expandAll`/`collapseAll` (`:401-420`), and every handler (`:422-620`) into the hook, re-pointing their `invoke(...)` calls at `lib/ipc/speakers.ts` from change 08 (`list_voiceprints`, `speaker_storage_stats`, `list_speakers`, `get_voiceprint_audio`, `get_meeting_audio_path`, `reject_voiceprint`, `verify_voiceprint`, `verify_speaker`, `reconfirm_voiceprint`, `rematch_meeting_speakers`, `preview_replace_speaker`, `replace_speaker`, `clear_all_voiceprints`, `purge_unconfirmed_caches`), returning every value and handler the render function uses; verify with `npx tsc --noEmit -p .`.
- [ ] 2.3 Rewrite `frontend/src/components/VoiceprintBrowser.tsx` to call `useVoiceprintActions()` and render the table/group JSX (`:649-957`) plus the 4 imported dialogs (`:958-991`), keeping the default export and zero-prop signature unchanged; verify with `npx tsc --noEmit -p .` and `npx next lint`.
- [ ] 2.4 Manual smoke test: open the Voiceprints browser, exercise each of the 4 dialogs (assign a person via the picker, replace a speaker, clear all voiceprints, purge unconfirmed caches) and confirm each still refreshes the list afterward; verify by checking off each dialog in this task's PR description.

## 3. Remove `any` from hooks/

- [ ] 3.1 Fix `frontend/src/hooks/useAutoScroll.ts:6,185` (`segments: any[]`, `(s: any) =>`) by typing `segments` against the actual shape read (an object with `id`); verify with `npx tsc --noEmit -p .` and `npx next lint src/hooks/useAutoScroll.ts`.
- [ ] 3.2 Fix `frontend/src/hooks/useImportAudio.ts:176,195,236,256` (`catch (err: any)`) by dropping the annotation and using `err instanceof Error ? err.message : String(err)` at each site; verify with `npx next lint src/hooks/useImportAudio.ts`.
- [ ] 3.3 Fix `frontend/src/hooks/useRecordingStateSync.ts:50` and `frontend/src/hooks/useRecordingStop.ts:545,551` (`(window as any).__TAURI__` / `.handleRecordingStop`) with a shared local `WindowWithTauriHooks` interface per file; verify with `npx next lint src/hooks/useRecordingStateSync.ts src/hooks/useRecordingStop.ts`.
- [ ] 3.4 Fix `frontend/src/hooks/useTranscriptRecovery.ts:170-175` (5× `(t as any).<field>`) by typing the mapped source value once instead of casting per field; verify with `npx tsc --noEmit -p .` and `npx next lint src/hooks/useTranscriptRecovery.ts`.
- [ ] 3.5 Fix `meeting: any` in `frontend/src/hooks/meeting-details/useCopyOperations.ts:9`, `useMeetingData.ts:9`, `useMeetingOperations.ts:6`, `useSummaryGeneration.ts:54` by importing `CurrentMeeting` from `@/components/Sidebar/SidebarProvider` (already imported elsewhere in `useSummaryGeneration.ts`) in place of `any`; verify with `npx tsc --noEmit -p .`.
- [ ] 3.6 Fix `frontend/src/hooks/meeting-details/useCopyOperations.ts:124,140` and `useMeetingData.ts:76,84` (`as any` / `let formattedSummary: any`) by typing against `Summary`/`SummaryDataResponse` from `@/types`; verify with `npx tsc --noEmit -p .`.
- [ ] 3.7 Fix `frontend/src/hooks/meeting-details/useModelConfiguration.ts:27,51` (`invokeTauri(...) as any`) by calling `lib/ipc/settings.ts#getModelConfig`/`getCustomOpenAIConfig` from change 08 instead, dropping both casts; verify with `npx tsc --noEmit -p .`.
- [ ] 3.8 Fix `frontend/src/hooks/meeting-details/useSummaryGeneration.ts:160,177,205,277,487` (`as any`/`as any[]`) by typing the polling result against `Summary`/`SummaryDataResponse` and calling `lib/ipc/models.ts#getOllamaModels` from change 08 for the `:487` site; verify with `npx tsc --noEmit -p .`.
- [ ] 3.9 Run `grep -rn ": any\|as any" frontend/src/hooks` and confirm it returns no matches (the remaining `useSummaryGeneration.ts:300,328,333` sites are removed by task 4.2 below, not here — if task 4 is done first, confirm zero matches after both); verify by inspecting the (empty) output.
- [ ] 3.10 Run `cd frontend && npx next lint` and confirm no `@typescript-eslint/no-explicit-any` error remains under `src/hooks/`; verify by grepping the lint output for `src/hooks` (errors elsewhere, e.g. `src/components`, `src/app`, are expected and out of scope — see design.md Migration Plan follow-up).

## 4. Pure-function extraction and first tests

- [ ] 4.1 Create `frontend/src/lib/transcript-formatting.ts` exporting `dedupeAndInsertTranscript(prev: Transcript[], next: Transcript): Transcript[]` (the exists-check-then-sort logic at `TranscriptContext.tsx:675-686`) and `formatTranscriptForClipboard(transcripts: Transcript[]): string` (the `formatTime`-and-join logic at `:702-712`); update `TranscriptContext.tsx`'s `addTranscript`/`copyTranscript` to call these; verify with `npx tsc --noEmit -p .`.
- [ ] 4.2 Create `frontend/src/lib/summary-formatting.ts` exporting `isLegacySummaryEmpty(sections: [string, unknown][]): boolean` (the `allEmpty` check at `useSummaryGeneration.ts:300`) and `formatLegacySummaryData(sectionOrder: string[], summaryData: Record<string, unknown>): Summary` (the loop at `:317-349`, using the same `typeof`/`'title' in`/`'blocks' in` narrowing already at `:327` instead of `any`); update `useSummaryGeneration.ts` to call these; verify with `npx tsc --noEmit -p .`.
- [ ] 4.3 Add `frontend/tests/lib/transcript-formatting.test.ts` covering: a new transcript is appended and the list stays sorted by `sequence_id`; a transcript with the same `text`+`timestamp` as an existing one is dropped; `formatTranscriptForClipboard` produces the `[MM:SS] text` format and `[--:--]` for an `undefined` `audio_start_time`; verify with `npx bun test tests/lib/transcript-formatting.test.ts`.
- [ ] 4.4 Add `frontend/tests/lib/summary-formatting.test.ts` covering: `isLegacySummaryEmpty` is `true` only when every section's `blocks` is missing or empty; `formatLegacySummaryData` preserves `_section_order`, defaults a missing `title` to the section key, trims block `content`, and sets `color: 'default'` on every block; verify with `npx bun test tests/lib/summary-formatting.test.ts`.

## 5. Final verification

- [ ] 5.1 Run `cd frontend && npx tsc --noEmit -p .`; verify no new errors versus the pre-change baseline.
- [ ] 5.2 Run `npx next lint`; verify `src/hooks/**` and every new `src/components/ModelSettings/**`, `src/components/Voiceprints/**`, `src/lib/{transcript,summary}-formatting.ts` file reports zero `@typescript-eslint/no-explicit-any` errors.
- [ ] 5.3 Run `npx bun test tests/`; verify the new tests pass and no previously-passing test regresses (the 2 pre-existing failures tracked in change 02 are unaffected by this change).
- [ ] 5.4 Confirm `frontend/src/components/VirtualizedTranscriptView.tsx` has no diff in this change; verify with `git diff --stat` showing the file absent from the changeset.
- [ ] 5.5 Run `openspec validate 09-frontend-split-large-components --strict`; verify it passes.
